{
  "name": "vault-api-read",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@open-care/vault-core": "workspace:*",
    "@open-care/vault-db": "workspace:*",
    "hono": "^4"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
